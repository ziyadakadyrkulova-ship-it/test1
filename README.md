20. Две группы по числовому баллу
Сквозная задача: файл scores_groups.csv: колонки group (две метки, например A и B) и score (числовой балл). Сравнить средние, разброс и распределения между группами без разбиения на train/test и без обучения модели.

9: Загрузить score и group в массивы; по маскам выделить два массива баллов — только для группы A и только для B; посчитать в NumPy среднее и стандартное отклонение в каждой группе.

10: На тех же двух массивах — общие границы для гистограмм: min/max по объединённым данным, затем np.histogram для каждой группы с одинаковыми bins (или одинаковое число бинов).

11: Pandas: groupby('group')['score'].agg(['mean', 'std', 'count']), сохранить group_stats.csv.

12: На той же таблице — одно число «разность средних» (среднее A − среднее B или наоборот — зафиксировать в отчёте); при необходимости отфильтровать выбросы простым правилом (опционально, в рамках того же файла).

13: Две гистограммы score рядом (subplot) с общей шкалой по оси X или boxplot по group; PNG.

14: POST CSV того же формата → JSON: средние и числа наблюдений по группам, разность средних (как в н.11–12).
